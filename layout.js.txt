import './globals.css';
import Navbar from '../components/Navbar';

export const metadata = {
  title: 'Biip Flowerhorn Aquatic - Ikan Louhan Premium',
  description: 'Dari Hobi Menjadi Mahakarya. Penyedia ikan Louhan berkualitas sejak 2020.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body className="min-h-screen flex flex-col bg-biip-light">
        <Navbar />
        <main className="flex-grow">{children}</main>
        {/* Footer akan kita tambahkan di sini nanti */}
      </body>
    </html>
  );
}